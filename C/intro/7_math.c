#include <stdio.h>
#include <math.h>



int main() {
  // If you ever need any math functions, C provides a math library!
  double a = sqrt(9); // 3
  double b = pow(2,4); // 16
  int c = round(3.14); // 3
  int d = ceil(4.14); // 5
  int e = floor(3.99); // 3
  double f = fabs(-100); // 100, absolute value
  double g = log(3); // log of 3
  double h = sin(45);
  double i = cos(45);
  double j = tan(45);  

}