package com.knguyendev.quickstart;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Creates a test suite. You'll notice that our spring application starts up
 *
 */

@SpringBootTest
class  QuickstartApplicationTests {

	/**
	 * Create a singular test. So this actually creates a test version of our spring boot application
	 * and tests it.
	 *
	 * For this particular test, it tests if our spring boot app actually starts up.
	 */
	@Test
	void contextLoads() {
	}

}
